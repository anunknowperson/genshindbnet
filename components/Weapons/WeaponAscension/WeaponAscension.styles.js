
import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
    
    container: {
        width: '100%', padding: '10px 20px 0px 0px', display: 'flex', flexWrap: 'wrap', gap: '10px', marginBottom: '20px',
    }

}));