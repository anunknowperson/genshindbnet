
import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
    

  weaponSetNameHeader: {
    color: theme.white,
    fontSize: '48px',
    fontWeight: 500,
    margin: '0px 0px 20px 0px',
  },

  
}));
